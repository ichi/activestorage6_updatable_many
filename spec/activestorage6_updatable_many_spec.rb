RSpec.describe Activestorage6UpdatableMany do
  it "has a version number" do
    expect(Activestorage6UpdatableMany::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
